# License notice

The `model_checker/automata` package is vendored from
[`automata-lib-vitamin`](https://github.com/VincenzoPalma/automata-lib-vitamin),
a standalone, logic-agnostic automata/game-solving library. It is
distributed under the **MIT License** (see `LICENSE` in this directory),
which is **different** from this repository's own license (see the top-level
`LICENSE` file). Only the code under `model_checker/automata/` is covered by
the MIT terms; the rest of this repository is not.
